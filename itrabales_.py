#iterables in python
#iterables are objects that can be iterated over
#iterables are like lists, tuples, sets, dictionaries, strings
#iterables are like containers
#iterables are like boxes that contain items
#iterables are like shelves that contain books
#iterables are like folders that contain files
#iterables are like anything that contains items
#iterables are like anything that contains data
#iterables are like anything that contains information

user = {
    'name': 'Golem',
    'age': 5006,
    'can_swim': False
}

# for item in user.items():
#     print(item)

# for item in user.values():
#     print(item)

# for item in user.keys():
#     print(item)

for key, value in user.items():
    print(key, value)


